1) Header Section (Big Screens)

Header should be responsive.

On large screens (desktop/laptop):

Place navigation links (Home, About, Contact), Search bar, and Dark/Light toggle button all on a single line horizontally (not stacked).

Layout order:

Left: Logo (icon image).

Middle: Navigation links (Home | About | Contact).

Right: Search bar + Dark/Light toggle.

On small screens (mobile/tablet):

Collapse navigation links into a hamburger menu.

Search + Dark/Light toggle remain visible at the top.

2) Hero Section (Typing Effect Update)

Background: main hero image (blurred, centered, no crop from top).

Text: “Welcome to CineVerse – Your Gateway to the World of Cinema!”

Gradient colors: Pink → Aqua → Deep Yellow.

Typing animation behavior:

Text should type slowly (one letter at a time).

Once fully typed → remain visible for 5 seconds.

Then delete quickly (fast speed, letter by letter).

Repeat loop infinitely (slow typing, pause, fast delete).

3) Footer Section (Features + Connect)
(A) Features Links

Add a Features list in the footer.

Each item should be a clickable link (<a> tag) that scrolls/navigates to the correct section:

Trending Content → scrolls to trending movies section.

Popular Trailer → scrolls to trailer carousel section.

Regional Movie → scrolls to Bollywood section.

Recommendation → scrolls to home (movie details recommendation area).

(B) Connect Section

Add social media icons: Twitter (X), Facebook, Instagram, YouTube.

Icons must be clickable and open these links:

Twitter → https://x.com

Facebook → https://facebook.com

Instagram → https://instagram.com

YouTube → https://youtube.com

Icons should be styled with hover effects (scale up or change color).

4) About Section Update

Remove “Meet the Developer” section entirely.

Only keep:

Overview of CineVerse features.

Attribution: “Powered by TMDb”.

5) Trailer Playback

In the Popular Trailers Section and Movie Details Page trailers:

Do not redirect to YouTube.

Instead, embed the trailer and play directly inside the website using YouTube’s iframe embed player.

Provide play/pause controls.

Must be responsive (resize properly for mobile).