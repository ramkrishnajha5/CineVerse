CineVerse – Full Project:
Let’s write a final complete prompt that includes:
Note:
Your TMDb API key (2f0c4d355398a7cb7b60f0ffdb48222e).

This is only frontend and api integration project so kindly use only front end tools make in react+js/ts+tailwind dont use/create server side backend and database this will unnecessary.

1) Header Section

The header should always remain visible (sticky at the top when scrolling).

It must contain three key elements:

Logo:

Use the logo image(CineVerseLogo.png) you provided (icon only, no text).

Remove all previous versions of the logo.

The logo should be slightly larger for visibility but remain responsive (scales properly on different devices).

Search bar:

Positioned in the header.

Implements debounced search (800ms delay) so the API isn’t called on every keystroke.

Behavior:

If the user types fewer than 3 letters → no API request is made.

Once 3 or more letters are typed and the user pauses for 800ms → call the TMDb multi-search endpoint.

If the user types again before 800ms ends, reset the timer and cancel the previous request.

Display search results in a dropdown list showing:

Title of the result.

Type label (Movie / TV Show / Web Series).

Optionally the release year.

When the user clicks a result → redirect to the appropriate Details Page.

Dark/Light mode toggle button:

Animated and interactive toggle button in the header.

Should morph between sun ↔ moon icon with smooth animation (250–400ms).

Transition between themes should also be smooth for the entire page.

Store user preference in localStorage so the chosen theme is remembered.

Respect the user’s system setting (prefers-color-scheme) on first load.

Ensure all sections (hero text, cards, footer, forms) adapt properly in both light and dark themes with accessible contrast.

2) Hero Section

Placed immediately below the header.

Background:

Use the provided hero background image(CineVerseMain.png).

Must always remain centered and never crop from the top in any orientation (mobile/landscape/tablet/desktop).

Apply a soft blur effect (via CSS filter or overlay) so that the text above is easy to read.

Overlay text:

Content: “Welcome to CineVerse – Your Gateway to the World of Cinema!”

Text styling:

Gradient color mix of Pink → Aqua → Deep Yellow applied across the text.

Gradient should look vivid in both dark and light themes.

Animation:

Typewriter effect: letters typed one by one.

Once the full text appears, keep it visible for 5 seconds.

Then delete the text one letter at a time.

Repeat this process infinitely in a smooth loop.

Optionally, a blinking cursor effect should be added for realism.

3) Home Page Sections (in this exact order)
(A) Trending Content Section

Appears right after the hero section.

Contains the following categories:

Trending Movies (Daily & Weekly) → fetch data from /trending/movie/day and /trending/movie/week.

Trending TV Shows (Daily & Weekly) → fetch from /trending/tv/day and /trending/tv/week.

Trending Web Series → classify relevant TV entries from TMDb as “web series” if applicable.

Display rules:

Each item (movie, TV show, web series) should be displayed as a card.

Card must include: poster image, title, and rating (vote_average).

Clicking on a card opens its Details Page.

Cards should be arranged in a responsive grid or carousel depending on screen size.

(B) Popular Movies by Region Section

Fetched from TMDb API using region filters.

Categories:

Hollywood → with_origin_country=US.

Bollywood → with_origin_country=IN.

Tollywood → with_origin_country=IN, with filtering for Telugu/Tamil movies if possible.

Display as cards (poster, title, rating).

Clicking a card → Details Page.

(C) Popular Web Series by Region Section

Use TMDb TV endpoints to fetch popular shows that can be categorized as Web Series.

Regional categorization (similar to movies):

Hollywood Web Series → US-based.

Bollywood Web Series → India-based.

Tollywood Web Series → Telugu/Tamil based if filtering possible.

Display as cards (poster, title, rating).

Responsive grid or horizontal scroll on smaller screens.

Clicking a card → opens Details Page.

(D) Popular TV Series by Region Section

Again, use TMDb TV endpoints.

Categorize TV shows by country/region:

Hollywood TV Series (with_origin_country=US).

Bollywood TV Series (with_origin_country=IN).

Display same as above (cards with poster, title, rating).

Cards link to Details Page.

(E) Popular Trailers Section

Fetch from /movie/{id}/videos.

Extract YouTube trailer links (filter type = "Trailer").

Display trailers in a slider/carousel format with embedded YouTube players.

Slider should be touch/swipe friendly for mobile.

4) Movie / Series Details Page

Each Movie/TV/Web Series should have a dedicated Details Page.

Fetch data from /movie/{id} or /tv/{id}, plus /credits and /videos.

Display the following information:

Poster (large, high resolution).

Title (movie/series name).

Trailer (embedded YouTube from /videos).

Rating (vote_average with star/number format).

Storyline (overview).

Genres (list of genres like Action, Drama, Comedy).

Runtime (movie length or TV series episode runtime).

Release Date (or first_air_date for TV).

Earnings (budget & revenue for movies).

Cast (top-billed actors with photos if available).

Crew (director, writer, etc.).

Below main details → “More Like This” section:

Show related items with same genre(s) and release year.

Display as scrollable horizontal card list.

5) About Us Page (Separate Page)

Route: /about.

Content should include:

Overview of CineVerse features:

Explore Movies (Hollywood, Bollywood, Tollywood).

Discover Trending content (movies, TV shows, web series).

Watch popular trailers.

Access detailed cast, crew, genres, ratings, and storyline.

Attribution: “Powered by TMDb” (with their official logo if allowed).

Developer credit: “Developed by Ram Krishna Jha.”

Page design should be clean, with large readable typography and responsive layout.

6) Contact Us Page (Separate Page)

Route: /contact.

Contact form must include:

Name (text field).

Surname (text field).

Email (email field with validation).

Message (multi-line textarea).

Send Message button.

On submission:

Validate required fields.

Validate email format.

Show either success message or error message (client-side only unless backend is added).

7) Footer (Improved, Consistent Across All Pages)

Footer must appear on every page (Home, Movie Details, About, Contact).

Must contain:

Navigation links: Home | Movie Details | About Us | Contact Us.

Copyright line:

© 2025 CineVerse. All rights reserved.
Powered By TMDB
Made With ❤️ By Ram Krishna

Clean, decent design that adapts to both dark and light modes.

On smaller screens → links stack vertically.

Should have consistent spacing and alignment with the rest of the site.

8) Global Rules (Apply Everywhere)

Responsiveness:

Mobile-first design.

Key breakpoints: ~375px, 768px, 1024px, 1280px.

Grids and carousels should reflow properly on smaller screens.

Accessibility:

Use semantic HTML tags (header, main, section, footer, nav, form).

Keyboard navigable (focus styles visible).

All images should include alt text.

Maintain contrast ratios for readability in both themes.

Dark/Light Mode:

Smooth transition with CSS variables.

Gradient hero text (pink, aqua, deep yellow) must look good in both themes.

Performance:

Debounced search ensures fewer API calls.

Use lazy loading for images and videos where possible.

Consistency:

Same header (logo, search, dark/light toggle) on all pages.

Same footer on all pages.